resources:
  pipelines:
    pipeline_test:
      name: test
      libraries:
        - notebook:
            path: ../src/test/bronze
        - notebook:
            path: ../src/test/silver
        - notebook:
            path: ../src/test/gold
      schema: sales
      photon: true
      catalog: products
      serverless: true