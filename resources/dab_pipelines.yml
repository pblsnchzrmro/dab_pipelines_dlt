resources:
  pipelines:
    pipeline_test:
      configuration:
        env: dev
      name: test
      libraries:
        - notebook:
            path: ../src/test/bronze.ipynb
        - notebook:
            path: ../src/test/silver.ipynb
        - notebook:
            path: ../src/test/gold.ipynb
      schema: sales
      photon: true
      catalog: products
      serverless: true